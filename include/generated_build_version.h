#pragma once

// GENERATED FILE - rewritten fresh before every ESP32-S3 build by
// scripts/generate_build_version.py (wired in via platformio.ini's
// extra_scripts). This checked-in copy is only a placeholder fallback
// (see build_version.cpp) for a build path that somehow skips that
// script - a real `pio run -e esp32s3` always overwrites this with the
// current build's actual timestamp before compiling anything. Gitignored
// (see .gitignore) so a real build touching this file doesn't show up as
// a source change in every `git status` afterward.
#define FIRMWARE_BUILD_VERSION "dev"
